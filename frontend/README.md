# Frontend (GitHub Pages)

1) Update `config.js` with your Firebase config and Render API URL.
2) Deploy the `frontend` folder to GitHub Pages.

The site is a static app that calls the FastAPI backend and uses Firebase Auth + Firestore for history.
